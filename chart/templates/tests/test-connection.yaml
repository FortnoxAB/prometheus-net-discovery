---
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-net-discovery.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-net-discovery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-net-discovery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
