version: "3.0"
services:
    prometheus:
        image: <image-name:latest>
        container_name: prometheus
        environment:
            - Subnet=10.160.77.0/24 
        volumes:
            - ./prometheus.yml:/etc/prometheus/prometheus.yml:z
            - /prometheus
        ports:
            - "9090:9090"
